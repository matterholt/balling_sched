[project]
name = "balling-sched"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2.2",
    "django-template-partials>=25.1",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
